<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MenuActivity"
    android:background="#4FC3F7">

    <!-- SpaceRace Title -->
    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/Game_Title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="SpaceRace"
        android:textSize="70sp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="48dp" />

    <!-- Button for Tilt Mode -->
    <Button
        android:id="@+id/button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Tilt Mode"
        android:layout_below="@id/Game_Title"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="24dp"
        android:background="#3F51B5"
        android:textColor="#FFFFFF"
        android:textSize="18sp"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingTop="12dp"
        android:paddingBottom="12dp"
        android:elevation="4dp"
        android:stateListAnimator="@null" />

    <!-- Button for Slow Mode -->
    <Button
        android:id="@+id/button2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/button"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="24dp"
        android:background="#FF5722"
        android:elevation="4dp"
        android:paddingStart="16dp"
        android:paddingTop="12dp"
        android:paddingEnd="16dp"
        android:paddingBottom="12dp"
        android:stateListAnimator="@null"
        android:text="Slow"
        android:textColor="#212121"
        android:textSize="18sp" />

    <!-- Button for Fast Mode -->
    <Button
        android:id="@+id/button3"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Fast"
        android:layout_below="@id/button2"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="24dp"
        android:background="#4CAF50" />

    <!-- Button for High Scores -->
    <Button
        android:id="@+id/button_high_scores"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="High Scores"
        android:layout_below="@id/button3"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="24dp"
        android:background="#FF4081"
        android:elevation="4dp"
        android:paddingStart="16dp"
        android:paddingTop="12dp"
        android:paddingEnd="16dp"
        android:paddingBottom="12dp"
        android:stateListAnimator="@null"
        android:textColor="#FFFFFF"
        android:textSize="18sp" />

    <!-- TextView for "Enter Name" -->
    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/text_enter_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Enter Name:"
        android:textSize="30sp"
        android:layout_below="@id/button_high_scores"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="24dp" />

    <!-- TextView for Writing Name -->
    <EditText
        android:id="@+id/edit_text_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:hint="Your Name"
        android:layout_below="@id/text_enter_name"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="30dp"
        android:textSize="18sp" />

    <!-- TextView for Score -->
    <TextView
        android:id="@+id/score"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/edit_text_name"
        android:layout_centerInParent="true"
        android:layout_marginTop="16dp"
        android:text="Score : "
        android:textSize="46sp" />

    <!-- TextView for Score Number -->
    <TextView
        android:id="@+id/score_num"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/score"
        android:layout_centerInParent="true"
        android:layout_marginTop="16dp"
        android:textSize="46sp"
        tools:text="000 " />
</RelativeLayout>
